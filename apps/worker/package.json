{
  "name": "@rosapay/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx --env-file-if-exists=../../.env src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@rosapay/domain": "*",
    "@rosapay/postgres": "*",
    "@rosapay/stellar": "*",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/pg": "^8.23.1",
    "tsx": "^4.20.0",
    "vitest": "^3.2.0"
  }
}
