{
  "name": "@rosapay/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "db:migrate": "tsx src/migrate.ts"
  },
  "dependencies": {
    "@rosapay/domain": "*",
    "@rosapay/postgres": "*",
    "@rosapay/protocol": "*",
    "@rosapay/stellar": "*",
    "fastify": "^5.5.0",
    "pg": "^8.23.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/pg": "^8.23.1",
    "tsx": "^4.20.0",
    "vitest": "^3.2.0"
  }
}
