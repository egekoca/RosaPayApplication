{
  "name": "@rosapay/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=../../.env src/server.ts",
    "start": "tsx --env-file-if-exists=../../.env src/server.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "db:migrate": "tsx --env-file-if-exists=../../.env src/migrate.ts"
  },
  "dependencies": {
    "@noble/curves": "^2.3.0",
    "@rosapay/domain": "*",
    "@rosapay/postgres": "*",
    "@rosapay/protocol": "*",
    "@rosapay/secure-signer": "*",
    "@rosapay/stellar": "*",
    "fastify": "^5.5.0",
    "pg": "^8.23.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/pg": "^8.23.1",
    "tsx": "^4.20.0",
    "vitest": "^3.2.0"
  }
}
