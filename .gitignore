node_modules/
.DS_Store
*.log
.watchman-cookie-*
.env
.env.*
!.env.example
coverage/
dist/
build/

# React Native
apps/mobile/android/.gradle/
apps/mobile/android/app/build/
apps/mobile/android/app/*.keystore
apps/mobile/ios/Pods/
apps/mobile/ios/build/
apps/mobile/.bundle/

# Rust / Stellar
contracts/target/
.stellar/

# Editors
.idea/
.vscode/

# Demo account secrets created by `npm run testnet:demo-pair`.
config/testnet-demo-pair.json

# Vercel CLI link and the OIDC token it downloads. The `.env.*` rule above
# already covers `.env.local`, and re-adding `.env*` here would override the
# `!.env.example` exception.
.vercel
